Build Failed
Could not parse File as JSON: vercel.json

3:32 AM
{
  "version": 2,
  "builds": [
    {
      "src": "api/*.php",
      "use": "
@vercel/php
"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1.php"
    }
  ]
}
